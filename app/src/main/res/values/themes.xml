<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlokdemoApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AlokdemoApplication" parent="Base.Theme.AlokdemoApplication" />

    <!-- Cricbuzz Clone theme: modern minimal, light surfaces with a single green accent -->
    <style name="Theme.CricbuzzClone" parent="Base.Theme.AlokdemoApplication">
        <item name="colorPrimary">@color/cricbuzz_green</item>
        <item name="colorPrimaryContainer">@color/cricbuzz_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/surface</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowBackground">@color/background</item>
    </style>

</resources>
